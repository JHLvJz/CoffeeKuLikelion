import styled from "styled-components";

const OrderText = styled.p`
    position: absolute;
    text-align: center;
    line-height: 14px;
    color: white;

    
    font-size = 18px;
    font-weight = 600;
`;

export default OrderText;